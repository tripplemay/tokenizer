{
  "batch_id": "BL-HARNESS-REPORT-COMPAT",
  "fix_round": 1,
  "created_at": "2026-07-31T03:21:30Z",
  "waiting": null,
  "verdicts": [
    {
      "feature_id": "F001",
      "result": "PASS",
      "evidence": "Locked SHA 9974815f. Diff review of src/cli/harness.ts:145-148: no-remote repos get repoKey local:sha256:<sha256(normalizeWorkspacePath(root))>, raw root only feeds the one-way hash, display name from basename; remote repos keep normalizeGitRemote output unchanged. Evaluator-built temp-HOME fixture (3 fresh git repos under one projectRoots dir: 1 with origin, 2 local-only, each with progress.json + harness-rules.md): remote key byte-identical to pre-fix normalization (github.com/example/remote-proj); both local keys matched ^local:sha256:[0-9a-f]{64}$; repeat discovery returned the identical key for the same path and a different key for a different path; JSON.stringify(buildReport(localRepo)) contained neither the temp HOME nor the repo root. Focused CLI suite tests/cli/harness.test.ts 24/24 passed including the new opaque-key regression.",
      "steps_to_reproduce": "npx vitest run tests/cli/harness.test.ts; then a scratch tsx script that mkdtemp()s a fake HOME, git-init three repos (one with an origin remote, two without), drops progress.json/harness-rules.md into each, calls discoverHarnessRepos({projectRoots:[tmp]}) twice and buildReport() on a local-only repo, and asserts remote-key equality, ^local:sha256:[0-9a-f]{64}$ format, cross-discovery stability, per-path distinctness, and absence of the tmp absolute path in the serialized report (evaluator script kept outside the repo; all 8 assertions passed)."
    },
    {
      "feature_id": "F002",
      "result": "PASS",
      "evidence": "Fix-round-1 change verified in source: FORWARD_SLASH_UNC_PATH_PATTERN is tested against the raw summary before any feature.title masking (src/server/harness-mode-intent-api.ts:191,205-207), closing the prior-round hole. Evaluator's independent probe (not the repo tests) against safePersistedSummary on the locked SHA: ACCEPTED exact Harness commands (/plan, /build F002, run /verify then /dashboard, x/autodrive), safe routes (REST /v1/images/generations, POST /api/trip/generate, GET /v12/models/list), word-internal slashes (喜欢/不喜欢, 门禁/限制/计费, i2i/edits/MCP) and standalone prose slash (生成 / 取消); REJECTED //server/share, //api/x, x //host/share y, //v1/images, a //b/c, /Users/alice/private/repo, /home/bob/x, /srv/data, /tmp, C:\\Users\\alice, C:/Users/alice, \\\\server\\share, file:///etc/passwd, POST /api/../private, POST /v1/images/output.txt, /api/, /v0/x, path-shaped/unknown slash commands (/plan/private, /plan.txt, /unknown), raw channel markers (stdout:, prompt:), credentials (api_key=, Bearer, PRIVATE KEY block, ghp_ token) and embedded newline. Rejections throw code sensitive_summary_data (or invalid_string) and the error message never echoes the rejected input. The exception is title-scoped: the same probe shows errorSummary still rejects /api/trip/generate, /v1/images/generations, 生成 / 取消 and /plan. Unit suite tests/server/harness-mode-intent-api.test.ts 90/90 passed.",
      "steps_to_reproduce": "npx vitest run tests/server/harness-mode-intent-api.test.ts; plus evaluator scratch probe: npx tsx --tsconfig tsconfig.json <probe.ts> which imports safePersistedSummary from @/server/harness-mode-intent-api and asserts accept/reject for the 30+ shapes listed in evidence, exiting non-zero on any mismatch (printed ALL PROBES PASSED)."
    },
    {
      "feature_id": "F003",
      "result": "PASS",
      "evidence": "Rejection happens before any Prisma access: app/api/harness/report/route.ts parses and validates the whole body via parseReport (line 244) before the first prisma.project.findFirst (line 250) and $transaction (line 257); route tests tests/server/harness-report-mode-intent.test.ts (27/27) assert for 11 negative title shapes (incl. //server/share, //api/x, x //host/share y, POSIX/Windows/UNC/file URL paths, traversal, raw channel, credentials, newline) that status is 400 with code sensitive_summary_data, the body does not echo the title, and prisma.project.findFirst/$transaction were never called; a positive test proves repoKey local:sha256:<64 hex> plus the four live slash titles is accepted (200) and persisted with the opaque key. Focused run: 3 files, 141/141 passed. Full npm test: 53 files, 695 passed, 4 skipped. npm run verify (prisma generate + tsc --noEmit) green, npm run lint 0 warnings/errors, npm run build (next build) exit 0. git diff d106eab..9974815 touches only src/cli/harness.ts, src/server/harness-mode-intent-api.ts, tests and docs — no Prisma schema, UI, i18n, framework or AGENT_FEATURE_VERSION changes.",
      "steps_to_reproduce": "npx vitest run tests/server/harness-mode-intent-api.test.ts tests/server/harness-report-mode-intent.test.ts tests/cli/harness.test.ts (141/141); npm test (53 files / 695 passed / 4 skipped); npm run verify; npm run lint; npm run build — all executed by the evaluator on the locked SHA in a clean worktree with fresh npm ci; git diff --stat d106eab..HEAD for scope confirmation."
    },
    {
      "feature_id": "F004",
      "result": "PASS",
      "evidence": "Fresh-context independent acceptance: evaluator read the locked spec and the full diff d106eab..9974815 itself, re-ran every required check (above) rather than trusting handoffs, and reproduced the prior evaluator's finding as fixed — //server/share, //api/x and x //host/share y are now rejected pre-masking while the four real legal title shapes stay accepted. Temp-HOME fixture proved remote/local key behavior and zero absolute-path leakage in serialized reports. Evaluator also scanned the real local Harness fleet (read-only, shapes only): 9 harness projects found (7 with remote, 2 local-only, matching the spec's harness-console-demo/wearwhat story), 84 real feature titles passed through the fixed validator with 0 rejections. This schema-valid verdict is the deliverable; waiting=null, no push/deploy performed, no product source/tests/progress/features/framework files modified (git status clean apart from this artifact).",
      "steps_to_reproduce": "Fresh clone/worktree at 9974815f00c2a190440f06e90fdd5300dc8d3fe1 -> npm ci -> focused vitest (141/141) -> npm test (695 passed / 4 skipped) -> npm run verify / lint / build (all green) -> evaluator scratch probes for validator shapes and temp-HOME repo discovery (both pass) -> read-only scan of real local harness projects feeding every features.json title through safePersistedSummary('feature.title') (84 titles, 0 rejected) -> bash .claude/autonomous/validate-verdict-artifact.sh docs/test-reports/BL-HARNESS-REPORT-COMPAT-verdict.json."
    }
  ]
}