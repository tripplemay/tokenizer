{
  "batch_id": "BL-FW-RELEASE-CONTRACT",
  "fix_round": 0,
  "created_at": "2026-07-31T04:54:39Z",
  "waiting": null,
  "verdicts": [
    {
      "feature_id": "F001",
      "result": "PASS",
      "evidence": "Independent evaluator run on the source repo at locked commits 1408dae/0c27c01 (HEAD verified via git log; tree clean). harness/framework-releases.json: schema_version 1, 23 releases 1.0.0 -> 1.5.3, all strict 3-part SemVer without leading zeros, strictly increasing, no duplicates, valid ISO dates. VERSION == '1.5.3' == manifest last entry. CHANGELOG.md has 23 v1 headings matching the manifest bidirectionally (version+date), including backfilled v1.1.0/v1.2.0/v1.3.0 (2026-07-25). Evaluator re-ran 'python3 scripts/validate-framework-release-contract.py' -> exit 0. Script inspected: only read_text on manifest/VERSION/CHANGELOG, no writes, no network/credential/env access. Negative fixtures in a scratch copy each exited non-zero with targeted messages: empty releases; leading zero 1.05.3; duplicate 1.5.3; non-increasing 1.4.9 appended; VERSION=1.5.2 mismatch; changelog-only v1.6.0 heading; manifest-only 1.6.0. md5 of validated files identical before/after failing and passing runs (read-only confirmed). Shipped suite 'python3 tests/test-framework-release-contract.py' -> 12 tests OK (includes duplicate/empty/leading-zero/non-increasing/invalid-date 2026-02-30/VERSION-drift/changelog-one-sided cases). CI workflow .github/workflows/release-contract.yml runs validator + test suite on pull_request and on push to main (path-filtered to contract inputs). Caveat: push trigger is path-filtered; pull_request is unfiltered and covers all inputs, so drift cannot merge undetected.",
      "steps_to_reproduce": "1) git checkout 0c27c01 in the source repo. 2) python3 scripts/validate-framework-release-contract.py (expect exit 0). 3) python3 tests/test-framework-release-contract.py (expect 12 tests OK). 4) Copy manifest/VERSION/CHANGELOG to a scratch dir, mutate each drift class (empty list, leading zero, duplicate, non-increasing, VERSION mismatch, one-sided changelog), re-run validator with its root override (expect non-zero each time) and compare file checksums before/after. 5) cat VERSION; diff v1 headings in CHANGELOG.md against the manifest releases. 6) Inspect .github/workflows/release-contract.yml triggers."
    },
    {
      "feature_id": "F002",
      "result": "PASS",
      "evidence": "Distribution fixture suite in the source repo (tests/test-framework-release-contract.py, FrameworkReleaseDistributionTest added in 0c27c01) exits 0 and covers: fresh init materializes framework/harness/framework-releases.json byte-identical to source with harness.lock sha256/upstream entry and harness.json framework.version 1.5.3; legacy v1.5.2 project gains the manifest via subsequent sync as a new managed file; adopt --as 1.5.2 stays record-only then sync adds it; nested bootstrap (framework/bootstrap.sh) and flat bootstrap (root bootstrap.sh) both distribute it. Evaluator independently ran harness.sh init into a scratch project: distributed manifest sha256 equals source sha256 (3e60773a...9fdc), harness.lock records that checksum as sha256 and upstream, harness.json = 1.5.3, framework/VERSION and framework/CHANGELOG.md present. Mirror mechanism inspected: templates/claude/harness.sh FRAMEWORK_MIRROR recursively manages the harness/ directory, so the manifest ships via the generic mirror with zero special-casing (grep for framework-releases in the sync logic finds no bespoke copy rule); offline --from path therefore inherits it. Caveats: self-overwrite protection is exercised only implicitly (fixture sync runs the project's installed harness.sh, exit 0), and the fixture does not explicitly re-assert byte-identity of VERSION/CHANGELOG post-sync (they ride the same generic mirror path).",
      "steps_to_reproduce": "1) git checkout 0c27c01 in the source repo. 2) python3 tests/test-framework-release-contract.py (expect OK; covers init/sync/adopt-then-sync/nested/flat bootstrap in temp dirs). 3) Manually: run harness.sh init into a mktemp project, then sha256sum harness/framework-releases.json and <project>/framework/harness/framework-releases.json and compare; inspect the project's harness.lock entry for the manifest (sha256/upstream) and harness.json version. 4) Read templates/claude/harness.sh FRAMEWORK_MIRROR and entries() to confirm recursive mirror distribution with no special case."
    },
    {
      "feature_id": "F003",
      "result": "PASS",
      "evidence": "Verified in the locked tokenizer worktree at 094318a (tree clean). cmp + sha256: framework/harness/framework-releases.json byte-identical to the source manifest (3e60773a...9fdc). harness.json and harness.lock both record framework version 1.5.3; harness.lock managed entry for framework/harness/framework-releases.json has sha256 equal to the actual file hash. src/shared/framework-version.ts derives FRAMEWORK_RELEASES and LATEST_FRAMEWORK_VERSION solely from the static JSON import; repo-wide grep over src/ and app/ for version-literal arrays finds no second release list (only a JSDoc example). Change surface confirmed via git show --stat on e299cfb (framework mirror + harness.json/lock), 0da19ab (framework-version.ts + its tests + ModeBadges test) and 094318a (state files only): no Prisma schema/migration, API route, i18n, or deployment-config changes. Tests: tests/shared/framework-version.test.ts covers 1.5.3 -> latest, 1.5.2 -> behind count 1, 9.9.9 -> ahead, malformed 'abc'/'malformed' and leading-zero '01.0.3'/'1.00.3' -> unknown, v0.x '0.9.1' -> behind with behind=null (no-count compatibility), and asserts no hand-written array in source; tests/shared/mode-badges.test.ts renders latest/behind(behindN:1 + syncHint)/ahead(future-ahead text preserved) branches. Evaluator ran with a temporary node_modules symlink (removed afterwards, tree still clean) and vitest --no-cache: focused 2 files / 19 tests passed, exit 0; full suite 54 files, 701 passed / 4 skipped / 0 failed, exit 0; npm run verify (prisma generate + tsc --noEmit) exit 0; npm run lint exit 0 (no warnings); npm run build exit 0. Minor caveat: ModeBadges unknown render branch has no dedicated render test (its logic is covered by frameworkStanding unit tests).",
      "steps_to_reproduce": "1) In the tokenizer worktree at 094318a: cmp framework/harness/framework-releases.json <source-repo>/harness/framework-releases.json and compare sha256. 2) grep '\"version\"' harness.json harness.lock; compare the lock's sha256 for the manifest entry with sha256sum of the file. 3) grep -nE '\"1\\.[0-9]+\\.[0-9]+\"' src app (expect no duplicate release array). 4) ln -s <preinstalled tokenizer node_modules> node_modules; ./node_modules/.bin/vitest run --no-cache tests/shared/framework-version.test.ts tests/shared/mode-badges.test.ts; then ./node_modules/.bin/vitest run --no-cache; npm run verify; npm run lint; npm run build. 5) rm the node_modules symlink; git status --short must be empty."
    },
    {
      "feature_id": "F004",
      "result": "PASS",
      "evidence": "This evaluator ran in a fresh context at the locked tokenizer ref 094318a and independently inspected the source repo at 1408dae/0c27c01 without trusting the generator handoff: the handoff's claimed focused-test result (16 tests, exit 0 with --no-cache) was superseded by the evaluator's own runs (focused 19/19 including ModeBadges; full 701 passed / 4 skipped). Source validator re-run by the evaluator exit 0; fixture suite re-run 12 tests OK; negative drift fixtures reproduced non-zero exits; byte-identity, harness.json/lock 1.5.3, lock checksum, single-source consumer derivation, four-state plus v0.x semantics, ModeBadges rendered branches, verify/lint/build all independently reproduced as detailed in F001-F003 evidence. No product source, tests, framework files, state, config, commits, refs, deployments, databases, or external services were modified: only this verdict artifact was created; the temporary node_modules symlink used for local verification was removed (verified: worktree node_modules absent, git status clean, HEAD unchanged). No push/tag/deploy performed; no L2 resources touched. waiting=null: no authorization or adjudication boundary was hit.",
      "steps_to_reproduce": "1) Read progress.json, features.json, docs/specs/BL-FW-RELEASE-CONTRACT-spec.md, and the generator handoff from disk. 2) In the source repo at 0c27c01: run the validator and its 12-test suite, mutate scratch copies for each drift class, inspect .github/workflows/release-contract.yml, and run the distribution fixtures plus a manual harness.sh init byte-identity check. 3) In the tokenizer worktree at 094318a: cmp the manifest against source, check harness.json/harness.lock + lock checksum, grep for duplicate release lists, run focused and full vitest with --no-cache via a temporary node_modules symlink, run npm run verify/lint/build, then remove the symlink. 4) Validate this artifact against .claude/autonomous/verdict-artifact.schema.json."
    }
  ]
}